JWKS_URI: https://test-tenant.logto.app/oidc/jwks
JWT_ISSUER: https://test-tenant.logto.app/oidc
AUDIENCE: https://api.consensus.test

CORS_ORIGINS: http://localhost:5173

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://test-tenant.logto.app/oidc
          jwk-set-uri: https://test-tenant.logto.app/oidc/jwks
          audiences: https://api.consensus.test
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
